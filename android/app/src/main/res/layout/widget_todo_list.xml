<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_todo_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingBottom="4dp">

        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_typed"
            android:contentDescription="Typed" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:fontFamily="sans-serif-medium"
             android:text="@string/widget_todo_title"
             android:textColor="@color/widget_fg"
            android:textSize="13sp"
            android:letterSpacing="0.02" />
    </LinearLayout>

    <!-- Progress ("x/y done") is rendered by the collection itself as its
         first row: static-view pushes for collection widgets are deferred by
         Android 16's AppWidgetServiceImpl and may never be applied by the
         host, while collection re-binds deliver live. -->

    <ListView
         android:id="@+id/widget_todo_list"
         android:layout_width="match_parent"
         android:layout_height="0dp"
         android:layout_weight="1"
         android:divider="@android:color/transparent"
         android:dividerHeight="6dp"
         android:overScrollMode="ifContentScrolls"
         android:scrollbars="none" />

    <TextView
        android:id="@+id/widget_todo_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:fontFamily="sans-serif"
         android:text="@string/widget_empty"
         android:textColor="@color/widget_muted"
        android:textSize="13sp"
        android:visibility="gone" />
</LinearLayout>
